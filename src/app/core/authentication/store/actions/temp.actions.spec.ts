import * as fromAuthentication\store\actions\temp from './authentication\store\actions\temp.actions';

describe('loadAuthentication\store\actions\temps', () => {
  it('should return an action', () => {
    expect(fromAuthentication\store\actions\temp.loadAuthentication\store\actions\temps().type).toBe('[Authentication\store\actions\temp] Load Authentication\store\actions\temps');
  });
});
